# Project memory

This repo has no instructions of its own. Everything lives one level up, at the project
root `~/Projects/Resolve/`, beside this repo and `resolve-webapp/`.

- Project memory: `~/Projects/Resolve/` (`context.md`, `Agenda.md`, `Findings.md`, …)
- Shared rules: `~/Projects/Resolve/_shared/`
- This repo's notes: `~/Projects/Resolve/backend.md`

**Start by reading `~/Projects/Resolve/AGENTS.md`**, then
`~/Projects/Resolve/_shared/session-protocol.md`, and follow it. It says which files to
read, in what order, and when to update them.

Do not add rules to this file. General rules go in `~/Projects/Resolve/_shared/`;
project-specific rules go in `~/Projects/Resolve/`. `CLAUDE.md` in this repo stays the
single line `@AGENTS.md`.
